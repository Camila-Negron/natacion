import { Component } from '@angular/core';

@Component({
  selector: 'app-brazada',
  templateUrl: './brazada.component.html',
  styleUrls: ['./brazada.component.css']
})
export class BrazadaComponent {

  constructor() { }

  selectStyle(style: string) {
    // Manejar la selección de estilo de natación
  }

  goNext() {
    // Navegar a la siguiente vista
  }
}
